<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/editForm.css">
    <link rel="stylesheet" href="/css/app.css">
    <link rel="icon" href="images/favicon.png">
    <title>Edit Post</title>
</head>

<body>
    <div class="form">
        <h2>Edit Post</h2>
        <form class="g-3 needs-validation" novalidate action="/posts/<%= post._id %>?_method=PUT" method="POST">
            <div>
                <div class="input-group has-validation">
                    <input type="text" name="title" class="form-control title" id="validationCustomUsername"
                        aria-describedby="inputGroupPrepend" placeholder="Title" required value="<%= post.title %>">
                </div>
            </div>
            <div>
                <textarea name="content" class="form-control content validationCustom03" rows="12"
                    placeholder="Type your content here . . ." required><%= post.content %></textarea>
            </div>
            <div>
                <input class="tags form-control" name="tags" id="validationCustom04" placeholder="tags . . ."
                    value="<%= post.tags %>">
            </div>
            <div>
                <input class="image form-control" name="image" id="validationCustom04" placeholder="image url"
                    value="<%= post.image %>">
            </div>
            <div class="col-12">
                <button class="btn btn-primary" type="submit">Save</button>
            </div>
        </form>
    </div>
    <script src="formValidator.js"></script>
</body>

</html>